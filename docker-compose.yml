services:
  mysql:
    image: mysql:8
    env_file:
      - ./.env
    ports:
      - '13306:3306'
    command: --init-file /data/application/init.sql
    volumes:
        - ./init_db/init.sql:/data/application/init.sql
