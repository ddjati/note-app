apiVersion: v1
kind: Service
metadata:
  # annotations:
  #   kompose.cmd: kompose convert
  #   kompose.version: 1.34.0 (cbf2835db)
  # labels:
  #   io.kompose.service: mysql
  name: mysql
  labels:
    app: mysql
spec:
  type: NodePort
  ports:
    - name: "13306"
      port: 13306
      targetPort: 3306
      protocol: TCP
  selector:
    # io.kompose.service: mysql
    app: mysql
